# Copia este archivo a .env y rellena los valores. NUNCA subas .env al repositorio.

# Entorno
NODE_ENV=production

# Sesión (obligatorio en producción; genera uno: openssl rand -hex 64)
SESSION_SECRET=

# SMTP (Gmail u otro)
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Nombre <email@ejemplo.com>"
SMTP_TO=destino@ejemplo.com

# Admin inicial (solo se usa si no hay admins en la BD)
ADMIN_BOOTSTRAP_USER=
ADMIN_BOOTSTRAP_PASS=

# API (opcional; si lo defines, las rutas API requieren X-API-Key)
# API_KEY=

# Proxy (si está detrás de nginx/cloudflare: 1 o true)
TRUST_PROXY=loopback

# Login
LOGIN_WINDOW_MINUTES=15
LOGIN_LIMIT=20
LOGIN_LOCK_MAX=5
LOGIN_LOCK_MINUTES=15

# Tokens admin
ADMIN_TOKEN_TTL_HOURS=24

# Backups
BACKUP_RETENTION_DAYS=14

# Puerto (opcional; por defecto 3000). Railway lo asigna automáticamente.
# PORT=3000

# Directorio de datos (BD, backups, logs). En Railway usa el path del volumen.
# DATA_DIR=/app/data
